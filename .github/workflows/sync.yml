# name: Sync to Hosting Repo

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   sync:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Source Repo
#         uses: actions/checkout@v4
#         with:
#           path: source
#           persist-credentials: false
#           fetch-depth: 0

#       - name: Checkout Hosting Repo (blogs branch)
#         env:
#           HOSTING_REPO_PAT: ${{ secrets.HOSTING_REPO_PAT }}
#         uses: actions/checkout@v4
#         with:
#           repository: amit-kumar-saini1/toolskit.tech
#           ref: blogs
#           path: target
#           token: ${{ secrets.HOSTING_REPO_PAT }}
#           persist-credentials: false
#           fetch-depth: 0

#       - name: Sync code to Hosting Repo without rewriting history
#         env:
#           HOSTING_REPO_PAT: ${{ secrets.HOSTING_REPO_PAT }}
#         run: |
#           if [ -z "$HOSTING_REPO_PAT" ]; then
#             echo "HOSTING_REPO_PAT is not set"
#             exit 1
#           fi

#           git config --global user.name "sync-bot"
#           git config --global user.email "sync@bot.com"

#           rsync -a --delete --exclude '.git' --exclude '.github/workflows/sync.yml' source/ target/

#           cd target
#           git add -A

#           if git diff --cached --quiet; then
#             echo "No changes to sync"
#             exit 0
#           fi

#           git commit -m "Sync from source repo (${GITHUB_SHA})"
#           git push https://x-access-token:${HOSTING_REPO_PAT}@github.com/amit-kumar-saini1/toolskit.tech.git HEAD:refs/heads/blogs
